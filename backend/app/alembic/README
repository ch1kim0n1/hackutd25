Generic single-database configuration with async support for APEX.

To create a new migration:
    alembic revision --autogenerate -m "description of changes"

To apply migrations:
    alembic upgrade head

To revert last migration:
    alembic downgrade -1

To view current version:
    alembic current

To view migration history:
    alembic history
